const { entity, field } = require('gotu')

const <%= props.entity.name %> = 
        entity(<%= props.entity.name %>, {
            nickname: field(String),
            password: field(String)
            <%= props.entity.fields %>
        })

module.exports = User