const { entity, field } = require('gotu')

const <%= props.name %> = 
        entity('<%= props.name %>',{
            <%= props.fields %>
         })

const <%= props.name %>Input = 
         entity('<%= props.name %>Input',{
             <%= props.fields %>
          })

module.exports = { <%= props.name %>, <%= props.name %>Input }