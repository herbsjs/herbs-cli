const Repository = require('./baseRepository')
const { <%= props.name.camelCase %> } = require('../domain/entities/<%= props.name.camelCase %>')
const { Repository } = require('@herbsjs/herbs2mongo')
const database = 'herbs2mongo_testdb'

module.exports = class <%= props.name.pascalCase %>Repository extends Repository {
    constructor(mongoInstance){
        super({ 
            entity: <%= props.name.camelCase %>,
            collection: '<%= props.name.camelCase %>',
            ids: ['id'],  
            database,
            mongo: mongoInstance  
        })
    }
}