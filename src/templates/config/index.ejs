const env = require('sugar-env')
require('dotenv').config()

module.exports = {
  isProd: env.is('production'),
  graphql: require('./api'),
  rest: require('./api'),
  mongodb: require('./mongodb')
}
